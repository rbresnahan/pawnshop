# Character Work

Individual character work may live here when useful. Keep the convention loose and add only what the character needs. For example:

```text
characters/
└── character-name/
    ├── brief.md
    └── character.png
```

Revision folders, manifests, status files, numbered stages, and similar bookkeeping are not required. Git already provides history.

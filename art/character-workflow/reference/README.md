# Canonical Reference

Place the deliberately selected canonical reference character in this folder. It is the visual source of truth for the workflow and should not be overwritten or modified during consistency work.

A simple filename is recommended:

```text
canonical-reference.png
```

The reference is supplied by the user; this workflow does not fabricate, generate, move, or copy one into place automatically.
